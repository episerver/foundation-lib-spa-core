# Ignore build artifacts
/node_modules
/lib
package-lock.json
*.tsbuildinfo

# Ensure bin folder is always included, even if it's exluded by a higher-up .gitignore
!/bin

# Ignore OS artifacts
.DS_Store